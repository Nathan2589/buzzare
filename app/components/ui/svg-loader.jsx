import React from 'react';
import blender from '/blender.svg'; // Import the SVG file from the same directory as the component

const SVGImageExample = () => {
    return ( 
            <img src={blender} alt="Blender" />
    );
};

export default SVGImageExample;